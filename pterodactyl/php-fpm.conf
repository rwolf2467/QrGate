[www]
listen = 127.0.0.1:9000

; Keep env so config.php getenv() sees QRGATE_* vars.
clear_env = no

; Running rootless under Pterodactyl: php-fpm ignores the user/group directive
; when not started as root (harmless warning), so no override is needed here.
