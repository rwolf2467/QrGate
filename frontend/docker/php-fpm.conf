[www]
; Listen on TCP so nginx (same container) can reach php-fpm.
listen = 127.0.0.1:9000

; CRITICAL: by default php-fpm wipes the environment, so getenv() in config.php
; would not see the QRGATE_* vars injected by Docker. Keep them.
clear_env = no
